# Legendary Police Officer Bot

A Discord moderation bot with:
- Prefix commands (#)
- Slash commands (/)
- Button-based verification
- Button-paginated help
- Saved config in JSON
- Admin-only commands

## Setup
1. Install dependencies:
   npm install

2. Create a `.env` file using `.env.example`

3. Start the bot:
   npm start
